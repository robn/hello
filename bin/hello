#!/usr/bin/env perl

use 5.020;
use warnings;
use strict;

use IO::Async::Loop;
use Hello::World;

my $loop = IO::Async::Loop->new;

Hello::World->from_config(loop => $loop, filename => 'config.toml')->go->get;
